<div class="panel panel-default">
  <div class="panel-body">
    <div class="pull-right">
      <%= link_to 'Edit Customer', edit_customer_path(customer) %>
    </div>
    <% if customer.name.present? %>
      <%= link_to customer.name, customer %>
    <% else %>
      <%= link_to 'Customer', customer %>
    <% end %>
    <span class="stats">
      <span class="stat"><i class="fa fa-envelope"></i> <%= customer.message_count %></span>
      <span class="stat"><i class="fa fa-gbp"></i> £200</span>
    </span>
  </div>
</div>
